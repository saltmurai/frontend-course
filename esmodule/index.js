import person, { doABackflip, greeting } from "./person.js";

document.addEventListener("DOMContentLoaded", () => {
  const a = 4;
  const b = 5;
  console.log(person);
  greeting();
  doABackflip();
});

// REFACTOR
